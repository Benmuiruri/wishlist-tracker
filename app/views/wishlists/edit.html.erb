<main class="container">
  <%= link_to sanitize("&larr; Back to wishlist board"), wishlists_path %>
  <div class="header">
    <h1>Edit Wishlist</h1>
  </div>

  <%= turbo_frame_tag "wishlist_#{@wishlist.id}" do %>
    <%= render 'form', wishlist: @wishlist %>
  <% end %>
</main>